# コードベース実態分析レポート

生成日: 2025-11-01  
分析対象: `scripts/`ディレクトリ内の全ファイル  
分析方法: 実際のコードを読み込んで分析

---

## 📊 統計情報

### ファイル数
- TypeScriptファイル: 28個
- JavaScriptファイル: 16個
- 合計: 44個

### console.log使用状況
- **総使用箇所: 421箇所**
- `scripts/integrate/questions.ts`: 13箇所
- `scripts/check/duplicates.ts`: 7箇所
- `scripts/check/question-counts.ts`: 6箇所
- `scripts/check/env.ts`: 22箇所
- `scripts/test/firebase-functions.ts`: 10箇所
- `scripts/test/env.ts`: 多数
- その他: 多数

### any型使用状況
- `scripts/test/firebase-functions.ts`: `error: any`
- `scripts/check/env.ts`: `error: any`
- `scripts/check/question-counts.ts`: 型推論のみ（明示的でない）

---

## 🔍 発見された問題点

### 1. logger統一の問題 ⭐️⭐️⭐️

**現状:**
- `scripts/utils/logger.js`が存在し、統一的なログ機能を提供
- しかし、多くのスクリプトで未使用
- `console.log`が421箇所で使用されている

**影響:**
- ログ出力の一貫性がない
- 開発環境/本番環境での制御ができない
- 機密情報のマスキングが適用されない

**解決策:**
すべての`console.log/error/warn`を`logger`に置き換え

---

### 2. 重複ファイルの問題

**発見された重複:**
```
scripts/test-env.ts          ↔ scripts/test/env.ts
scripts/check-env.ts         ↔ scripts/check/env.ts
scripts/integrate-additional-questions.ts ↔ scripts/integrate/questions.ts
scripts/check-question-counts.ts ↔ scripts/check/question-counts.ts
scripts/duplicate-questions.ts ↔ scripts/check/duplicates.ts
```

**影響:**
- どちらが最新か不明確
- メンテナンスコストの増加

**解決策:**
古いパスのファイルを削除（新しいディレクトリ構造に統一）

---

### 3. 統合ロジックの複雑さ

**現状:**
`scripts/integrate/questions.ts`で複雑な正規表現による文字列操作：

```typescript
const additionalSection = /\/\/ 追加問題[\s\S]*?(?=\/\/|$)/m;
const arrayMatch = indexContent.match(/export\s+const\s+(\w+Questions):\s*Question\[\]\s*=\s*\[([\s\S]*?)\];/);
```

**問題点:**
- 正規表現が複雑で保守が困難
- エッジケースで失敗する可能性
- ASTパーサー未使用

**解決策:**
ASTパーサー導入、またはより堅牢な実装

---

### 4. 型安全性の不足

**現状:**
- `error: any`が複数箇所で使用
- 型推論に依存した箇所がある

**解決策:**
- `error: any` → `error: unknown`
- 明示的な型定義

---

## 🎯 推奨される改善作業

### 優先度: 高

1. **console.logをloggerに統一** (2-3時間)
2. **重複ファイルの削除** (30分)

### 優先度: 中

3. **統合ロジックの改善** (4-6時間)
4. **型安全性の強化** (1-2時間)

---

## 📝 詳細は `IMPROVEMENT_PROPOSAL.md` を参照

